{
  "name": "buffcodex",
  "version": "0.1.0",
  "private": true,
  "description": "A local Responses bridge that runs Codex tasks on all free Freebuff models with multi-account pooling.",
  "packageManager": "bun@1.4.0",
  "type": "module",
  "bin": {
    "buffcodex": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts serve",
    "accounts": "bun run src/cli.ts accounts",
    "models": "bun run src/cli.ts models",
    "doctor": "bun run src/cli.ts doctor",
    "test": "bun test tests/*.test.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "license": "MIT"
}
